node_modules/
.env
.netlify/
deno.lock

# agent secrets and lead data - NEVER commit (repo is public)
.campaign-secret
.github-token
campaign-audience-*.json
campaign-payload-*.json
customers.txt
.DS_Store

# admin tokens - NEVER commit (repo is public)
.leads-admin-token

# partner redemption codes - NEVER commit (repo is public)
# each line is a free copy of a paid bundle
apd-codes-*
*-codes-*.csv
*-codes-*.txt
