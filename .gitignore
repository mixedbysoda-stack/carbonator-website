node_modules/
.env
.netlify/
deno.lock

# agent secrets and lead data - NEVER commit (repo is public)
.campaign-secret
.github-token
campaign-audience-*.json
campaign-payload-*.json
customers.txt
.DS_Store

# admin tokens - NEVER commit (repo is public)
.leads-admin-token

# partner redemption codes - NEVER commit (repo is public)
# each line is a free copy of a paid bundle
apd-codes-*
*-codes-*.csv
*-codes-*.txt

# reseller activation key batches - NEVER commit (repo is public)
*-keys.txt
*-keys.csv

# local build helper, not part of the website
fix-tallboy-installer.sh

# TALLBOY installer fix working dir - pkgs, logs. NEVER commit (repo is public)
_tallboy-fix/

# creator program: ledger and payout statements carry creator + buyer emails. NEVER commit (repo is public)
creator-codes-ledger.csv
creator-payouts-*.csv
creators*.csv

# tooling installer log (install-tooling.command writes install.log here). NEVER commit
_tooling/

# Local preview server (never deploys)
_preview/
