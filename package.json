{
  "name": "carbonator-website",
  "version": "2.2.0",
  "private": true,
  "description": "Carbonator plugin website — Carbonated Audio",
  "scripts": {
    "check:tracking": "node scripts/check-tracking.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "resend": "^3.0.0",
    "stripe": "^14.0.0"
  }
}
